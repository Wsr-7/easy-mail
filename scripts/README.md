# Scripts

当前脚本：

```text
collect-outlook-mails.vbs    classic Outlook 采集器
run-sample-validation.ps1    sample digest + TypeScript 测试验证
```

说明：

- `collect-outlook-mails.vbs` 支持 `--sample`
- `run-sample-validation.ps1` 不依赖 Outlook 和 Copilot，可在本机先验证数据链路
